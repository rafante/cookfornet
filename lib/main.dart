import 'package:cookfornet/views/pagina_ingrediente.dart';
import 'package:flutter/material.dart';

void main() => runApp(CookForNetApp());

class CookForNetApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      theme: ThemeData.dark(),
      home: PaginaIngrediente(),
    );
  }
} //vamos memso hahahahah vamos sim vai veno hehe o veo vendo
